name: 04 - Using Actions

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - name: Checkout code
         uses: actions/checkout@v7
         with:
            node-version: 24.x
       - name: Printing Folders
         run: ls -la